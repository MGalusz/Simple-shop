@model Simple_shop.ViewModels.HomeVM
@using Simple_shop.Infrastructure
@{
    ViewBag.Title = "Praktyczne kursy";
}

<nav class="kategorieHorizontal">
    <ul>
        @foreach (var kategoria in Model.kategorie)
            {
            <li>
                <a href="@Url.Action("Lista", "Kursy", new {nazwaKategori = kategoria.NazwaKategorii.ToLower()})"
                   style="background-image: url(@Url.ikonyKategoriiSciezka(kategoria.NazwaPlikuIkony));">
                    @kategoria.NazwaKategorii
                </a>
            </li>
        }

    </ul>
    <a href="link">
        <img id="baner" src="../Content/Images/baner.png" />
    </a>
</nav>

<section>
    <ul class="KursyTrzyKolumny duzaListaKursy">
        @foreach (var best in Model.Bestsellery)
        {
            <li>
                <article>
                    <a class="dodajDoKoszykaLink" href="@Url.Action("DodajDoKoszyka", "Koszyk", new {id = best.KursId})">
                        <img src="../Content/Images/koszyk.png" />
                    </a>
                    <a class="kursSzczegolyLink"href="@Url.Action("Sczegoly", "Kursy", new { id = best.KursId})">
                        <div class="bestsellerFlag"></div>
                        <img class="kurs" src="@Url.ObrazkiiiSciezka(best.NazwaPlikuObrazka)" />
                        <h1>
                            @best.TytulKursu  -  @best.AutorKursu
                        </h1>
                    </a>
                </article>
            </li>
        }
    </ul>
</section>

<section>
    <ul class="KursyTrzyKolumny duzaListaKursy">

        @foreach (var news in Model.Nowosci)
        {
            <li>
                <article>
                    <a class="dodajDoKoszykaLink" href="@Url.Action("DodajDoKoszyka", "Koszyk", new {id = news.KursId})">
                        <img src="../Content/Images/koszyk.png" />
                    </a>
                    <a class="kursSzczegolyLink" href="@Url.Action("Sczegoly", "Kursy", new { id = news.KursId})">
                        <div class="newFlag"></div>
                        <img class="kurs" src="@Url.ObrazkiiiSciezka(news.NazwaPlikuObrazka)" />
                        <h1>
                            @news.TytulKursu - @news.AutorKursu
                        </h1>
                    </a>
                </article>
            </li>
        }
    </ul>
</section>



